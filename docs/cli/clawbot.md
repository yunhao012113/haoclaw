---
summary: "CLI reference for `haoclaw clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `haoclaw clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `haoclaw clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `haoclaw clawbot qr` (same behavior as [`haoclaw qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `haoclaw clawbot qr` -> `haoclaw qr`
